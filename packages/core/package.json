{
  "name": "composable-agents",
  "version": "0.1.0",
  "description": "Deterministic runtime for composable AI agents — sequence, signal, condition.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/types/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "build": "tsc",
    "lint": "biome check src/"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.22.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
